# Octop 使用说明书（2HELP.md）

> 面向最终用户、家庭管理员与小团队运维。版本对应 Octop 1.0.1。  
> 运行时数据默认在 `~/.octop/`（可用 `OCTOP_HOME` 覆盖）。  
> 更偏实现的说明见 `1DESIGN.md`；故障仍无法解决时，请带上 `octop version`、`~/.octop/config.json`（脱敏后）与相关日志。

---

## 1. 整体功能概述

Octop 是一个**自托管 AI 助手平台**：你在自己的电脑、NAS 或云主机上运行一个进程，就能同时得到 Web 控制台、命令行、即时通讯机器人、定时任务和可编程 HTTP/WebSocket API。它强调三件事：数据留在你这边、一个人可以拥有多个专家 Agent、家庭或小团队可以共用一套部署。

从使用者角度看，日常路径是：安装 → 打开控制台完成向导（管理员账号 + 模型供应商）→ 登录 → 在专家库创建一个 Agent（或使用默认助手）→ 开始对话。进阶路径包括：给 Agent 接上飞书/钉钉/企微/QQ 等通道、上传知识库文档、绑定腾讯文档或 Notion 等连接器、配置每天定时推送、打开浏览器/终端/远程桌面让 Agent 操作图形界面、通过 ACP 把编码任务委派给 Claude Code 或 OpenCode。

从管理员角度看，Octop 提供用户与邀请码、SSO、安全策略（工具审批、命令护栏、PII 脱敏）、审计日志、Token 用量与配额、备份恢复、Let's Encrypt 证书、插件市场、存储后端（本地磁盘 / COS / S3 等）以及系统更新。所有配置最终落到 `config.json`、环境变量与控制平面数据库，而不是散落在多个微服务里。

下面按「你能做什么」列出功能地图，随后每一节给出逐步操作、注意点、失败时的典型提示。

### 1.1 功能地图

| 能力 | 控制台入口 | CLI | 说明 |
|------|------------|-----|------|
| 对话 | `/chat` | `octop chats send/repl` | WebSocket 流式；支持附件、斜杠、轨迹 |
| 专家/Agent | `/experts`、Agent 配置 | `octop agent *` | 多 Agent，MBTI，欢迎语 |
| 定时任务 | `/tasks` | `octop cron *` | interval/cron/date |
| 连接器 | `/connectors` | — | OAuth / API Key / 自定义 MCP |
| 技能包 | `/skill-packages` | `octop skills *` | 全局包与 Agent 技能 |
| 知识库 | `/knowledge-bases` | — | RAG，可选 OCR/ONNX |
| 个性化 | `/personalization/*` | — | 技能、子代理、工具、插件、MBTI、记忆、通道 |
| ACP | `/acp` | `octop acp` | 入站 stdio + 出站 runner |
| 用量 | `/token-usage` | — | 个人 token 统计 |
| 终端/浏览器 | `/workbench/*` | — | AI 辅助 shell 与网页自动化 |
| 远程桌面/手机 | `/remote-desktop/*` | — | 屏幕流 + ADB |
| 模型管理 | `/admin/models` | `octop models *` `octop provider *` | 云模型、Ollama、语音 |
| 用户/SSO | `/admin/users` | `octop user *` | 邀请、权限、锁定 |
| 存储后端 | `/admin/backend` | — | COS/S3 等 |
| 插件 | `/admin/plugins` | `octop plugin *` | ZIP/URL 安装 |
| 安全 | `/admin/security` | — | 策略、护栏、审计 |
| 高级 | `/admin/advanced` | `octop backup/update` | 备份、TLS、更新、环境变量 |

### 1.2 角色与权限（使用前必读）

部署里至少有一个 **admin**。admin 可以管理用户、供应商、安全策略、备份、插件、存储后端。普通 **user** 拥有自己的 Agent、对话、连接器、知识库，默认不能改全局模型供应商。细粒度权限字符串由管理员在用户页勾选。被 `disabled` 的用户无法登录。登录失败次数过多会锁定一段时间，管理员可「解锁登录」。

### 1.3 数据存放位置

| 路径 | 内容 |
|------|------|
| `~/.octop/config.json` | 端口、时区、数据库、TLS、备份开关 |
| `~/.octop/env` | 环境变量文件 |
| `~/.octop/octop.db` | 默认 SQLite 控制平面 |
| `~/.octop/agents/<agent_id>/` | 该 Agent 工作区（文件、记忆、checkpoint） |
| `~/.octop/plugins/` | 已安装插件 |
| `~/.octop/cli_state.json` | CLI 默认用户与 Agent |
| `~/.octop/credential.txt` | 首次自动生成的管理员口令（若你未指定） |

请把整个 `~/.octop` 纳入备份。不要把该目录提交到 git，也不要在聊天里粘贴 `api_key`。

---

## 2. 安装与启动

### 2.1 环境要求

操作系统可以是 macOS、Linux 或 Windows。你**不必**预先安装 Python：官方安装脚本会用 uv 在 `~/.octop/venv` 准备 Python 3.12。建议机器有多核 CPU、数 GB 内存，以及足够磁盘存放数据库、工作区与知识库文档。若使用浏览器自动化，还需 Playwright 的浏览器内核（可用 extras 安装）。

### 2.2 一键安装

macOS / Linux：

```bash
curl -fsSL https://finnie-1258344699.cos.ap-guangzhou.myqcloud.com/octop/install.sh | bash
```

Windows PowerShell：

```powershell
irm https://finnie-1258344699.cos.ap-guangzhou.myqcloud.com/octop/install.ps1 | iex
```

安装后打开**新终端**或 `source ~/.zshrc` / `source ~/.bashrc`，确认：

```bash
octop version
```

`octop` 应出现在 `~/.octop/bin`。安装脚本不会改系统 Python。可用 `--extras` 追加浏览器或通道依赖，用 `--mirror` 指定 PyPI 镜像。

### 2.3 Docker

仓库提供 `docker/` 编排。将 `.env.example` 中与端口、管理员、数据库相关的键写入部署环境。容器必须挂载持久卷到 `/root/.octop` 或你设置的 `OCTOP_HOME`，否则重启会丢数据。生产建议同时配置反向代理与 TLS。

### 2.4 源码开发启动

贡献者克隆仓库后：`make install-hooks`、`uv sync`、`make dev` 或分别 `dev-backend` / `dev-frontend`。这不是家庭用户路径，但管理员若要打补丁需要知道：前端源码在 `dashboard/`，产物在 `src/octop/dashboard/`，不要手改产物。

### 2.5 启动服务

```bash
octop run
```

默认绑定与端口见 `config.json` / `OCTOP_PORT`（示例为 8088）。可用 `--host` `--port` `--log-level`。要用系统服务：`octop service start`（systemd/launchd）。Windows 下注意防火墙放行端口。启动后浏览器打开 `http://<主机>:<端口>/`。

### 2.6 首次向导

未完成 setup 时，控制台进入向导：选择或探测数据库（默认 SQLite，可改 PostgreSQL）、创建初始管理员、测试模型供应商、完成。向导完成前，其他 API 会返回需要 setup 的错误。若安装时已用 `octop init` 或环境变量 `OCTOP_ADMIN_USERNAME` / `OCTOP_DEFAULT_PASSWORD` 预置管理员，向导会相应缩短。自动生成的口令写在 `credential.txt`，请立即改密并删除该文件或限制权限。

### 2.7 登录与语言

打开控制台输入用户名口令。可选验证码（默认滑块，仅控制台）。界面语言跟随浏览器与用户偏好（中/英）。服务器显示的时间是 **config.json 的 default_timezone**，不是你笔记本的时区。若你人在东京、服务器设为 `Asia/Shanghai`，Cron「每天 9:00」按上海时间触发。

---

## 3. 每一个功能的详细使用说明

### 3.1 对话（Chat）

进入 `/chat`。左侧是 Agent 与线程列表，右侧是消息。输入框支持 Markdown、粘贴图片、上传附件（默认进入工作区 `inbound/`，大小受 `max_upload_mb` 限制，超限会提示附件过大）。发送后内容经 WebSocket 流式出现；工具调用会显示本地化后的工具名与提示行。

常用斜杠（输入 `/` 可见控制台目录）：

- `/help` 帮助
- `/status` 当前 Agent 与模型
- `/model` 切换模型
- `/new` 或 `/clear` 新对话
- `/compact` 压缩上下文
- `/stop` 停止生成
- `/history` `/token` 历史与用量
- `/list` `/switch` `/resume` 会话管理
- `/title` `/pin` `/delete` 线程元数据
- `/approve` `/reject` `/pending` 工具审批（当管理员开启 HITL）
- `/cron` `/skills` `/connectors` `/agent` 在允许的表面跳转或查询

中途可再发斜杠。HITL 恢复在控制台走专门的继续按钮。轨迹页可查看 turn 内事件，便于排障「为什么调用了 shell」。

**注意：** Agent 必须处于 running。若提示未运行，到 Agent 列表启动。若 failed，查看 `last_error`，常见原因是供应商密钥错误或工作区后端不可达。

### 3.2 创建与管理 Agent

在专家页选择模板（通用助手、办公自动化、运维、育儿、股票、新闻、CVM 医生等）或空白创建。可设置名称、简介、MBTI、系统提示、欢迎语、图标颜色。每个用户下名称不可重复。创建后请确认 default model 已指向你测试通过的供应商。

生命周期：启动、停止、重载。重载用于改技能或连接器后让运行时重建。删除不可恢复工作区，请先备份。共享 Agent（`is_shared`）给部署内其他用户使用时，仍应限制其工具范围。

CLI：

```bash
octop agent list
octop agent create --name 助手
octop agent from-expert general-assistant
octop agent use <agent_id>
octop agent start <agent_id>
octop agent stop <agent_id>
octop agent reload <agent_id>
octop agent delete <agent_id>
```

CLI 通过 `--user` / `--agent` 或 `octop config set-user`、`octop agent use` 记住默认对象。没有 `octop user login`：CLI 信任本机文件系统。

### 3.3 MBTI 人格

在个性化 → MBTI：可浏览 16 型、预览提示词、做问卷并应用到当前 Agent。人格会改写系统提示中的性格段落，不会改模型本身。若你随后手写覆盖了 system prompt，注意不要把人格段落整段删掉除非有意如此。

### 3.4 模型供应商

管理员在 `/admin/models` 添加 OpenAI 兼容端点、DashScope 等预设，或自定义 base_url 与 api_key。可拉取模型列表、测试连通、设置全局激活模型。本地 Ollama：`octop models ollama-list/pull/rm`。语音供应商分 STT/TTS。图片视频生成在媒体生成配置中。密钥只显示脱敏值。ChatGPT 账号类登录走 Codex OAuth 设备流（若界面提供）。

普通用户通常只能选择管理员已配置且对其可见的模型。若对话报供应商不可见或未配置，找管理员。

### 3.5 连接器（Connectors）

打开 `/connectors`，按目录添加实例：腾讯文档、腾讯会议、腾讯新闻、微云、IMA、微信读书、QQ 邮箱（IMAP 应用密码）、QQ 音乐、飞猪、百度地图、携程问道、美团旅游、滴滴、元典、有道云笔记、Notion、滴答清单、飞书 CLI、企微 CLI、WeKnora、Dify，以及自定义 MCP。OAuth 类需要控制台 HTTPS，否则会提示 OAuth 需要 HTTPS。API Key 类把密钥贴进表单即可。状态变为 active 后，在对话中 Agent 才能调用对应工具。可为 Agent 勾选默认打开的连接器。凭据加密存放，日志中不会打印明文。

### 3.6 IM 通道

在 Agent 个性化 → 通道，或通道管理页：选择 kind，填写各平台文档要求的 App ID/Secret/Token。保存后「测试」只验证凭证。QQ/企微/微信可能需要 `octop channel bind` 扫码。飞书可用 `octop channel feishu-setup`。群聊与私聊的 session_key 不同，斜杠在群里也可用（视平台权限）。把同一 Agent 绑多个通道是支持的，但请避免两个机器人互相回复造成循环。

### 3.7 定时任务

`/tasks` 新建：名称、触发器、prompt、task 类型（固定文本 or 让 Agent 生成）、是否新线程、模型覆盖、MCP。触发器示例：`interval:3600` 每小时；`cron:0 9 * * 1-5` 工作日 9:00（服务器时区）；`date:2026-12-31T09:00:00` 一次性。可立即执行。失败会记录 last_error。不要把每分钟调用大模型的任务设到没有配额的账户上。

### 3.8 知识库

管理员先打开知识库功能并配置 embedding（云或本地 ONNX）。用户创建知识库、上传 PDF/Word/PPT/Excel/文本，可选 OCR。文档状态从 pending 到 indexed。把知识库绑定到 Agent 或设为 default-open，对话时会自动检索并带引用。限额：库数量、每库文档数、单文档大小。改 embedding 模型后需要 reindex。进程重启会继续未完成索引。

### 3.9 技能与技能包

Agent 技能列表可启用/停用内置技能、从 SkillHub 搜索安装、导入 URL、手写 SKILL.md。全局技能包适合把一组技能复用到多个 Agent，支持 copy/push。远程工作区若后端不支持某些文件操作会报后端不支持。技能是给模型看的说明书加可选脚本，写得含糊会导致乱调用工具。

### 3.10 子代理

从 catalog 按 division（工程、设计、财务、测试等）挑选子代理定义，添加到当前 Agent。它们在编排场景下作为可委派角色。中英目录内容不同，界面语言会影响你看到的说明。

### 3.11 插件

管理员安装 bundled（天气、热榜、番茄钟、俄罗斯方块等，默认关闭）或第三方 ZIP/URL。然后在 Agent 上打开具体工具。插件可提供自定义 UI 卡片。只安装来源可信的插件：它们在 Agent 进程能力范围内运行。

### 3.12 记忆

在个性化 → 记忆查看原子记忆、事件、实体、episodes。可促进或拒绝候选记忆。便携记忆：pack 导出、adopt 导入、doctor 检查。换机器时先备份工作区再 adopt。不要把记忆功能理解成「无限长期记忆」：仍受检索与策略限制。

### 3.13 ACP

入站：本机执行 `octop acp --agent <id>`，在 IDE 里把该 stdio 服务器配成 Agent。出站：在 `/acp` 配置 runner 可执行文件路径，在 Agent 启用 acp_runner，对话中即可委派。权限门会要求确认。runner 未安装时测试会失败。详见仓库 `docs/acp.md`。

### 3.14 工作区文件

控制台提供树、读写、上传下载、glob、grep、重命名删除。路径相对于工作区。聊天附件在 `inbound/`。不要手动用系统文件管理器乱改 `.octop/` 系统目录。远程 COS 后端时，大文件列表走 probe，延迟与本地磁盘不同。

### 3.15 终端 AI+

`/workbench/terminal` 打开浏览器内终端，Agent 可协助解释报错、建议命令。仍受命令护栏约束。不要在共享屏幕场景把生产口令打进终端。

### 3.16 浏览器 AI+

`/workbench/browser` 启动有头/无头浏览会话，用于填表、截图、公开信息收集。空闲超时由 `browser_idle_timeout_minutes` 控制。需要已安装 Playwright 浏览器。录像回放若开放，可把流程沉淀为技能（路线图能力，界面以当前版本为准）。

### 3.17 远程桌面

Linux/Windows/macOS 可从控制台看屏幕并输入。无头 Linux 提供一键隔离桌面脚本。会话数有上限，超出会话限制错误。采集失败检查权限（macOS 辅助功能/屏幕录制）。这是远程办公能力，请仅在可信网络使用，并配合 TLS。

### 3.18 远程手机

若 `capabilities.mobile` 启用：连接物理机、Redroid 或模拟器。控制台可看画面、开 ADB shell。在 Agent 上绑定设备后，相关工具才可用。未启用时 API 不挂载。

### 3.19 用户管理与邀请

管理员创建用户、改角色、禁用、重置口令、解锁、赋权限。邀请码可设过期，用户打开 `/invite` 兑换。SSO 在用户页绑定。最后一个管理员不要降权或删除自己。邮箱格式错误会返回 EMAIL_INVALID。

### 3.20 安全设置

全局策略：HITL 开关、PII、工具黑白名单。命令护栏 YAML 可编辑与重置为出厂。审计日志按时间/操作者检索。生产环境建议打开 HITL 至少覆盖 shell 与文件删除类工具。

### 3.21 备份、TLS、更新、环境变量

备份：手动 `octop backup create` 或自动调度；恢复前确认 SQLite/PG 一致。TLS：填域名，preflight 后 issue。更新：检查通道、升级、重启。环境变量页可维护全局 env，敏感值显示星号。`OCTOP_CAPTCHA_SECRET` 等不会回传明文。

### 3.22 CLI 全命令速查

全局选项：`--user` `--agent` `--json` `-v`。

| 命令 | 用途 |
|------|------|
| `init` | 初始化库与管理员 |
| `run` | 前台启动 |
| `service` | 系统服务 |
| `config show/set-user` | CLI 状态 |
| `user *` | 用户 CRUD |
| `agent *` | Agent 生命周期 |
| `chats *` | 线程与发送 |
| `channel *` | 通道与扫码 |
| `cron *` | 定时任务 |
| `provider *` | 供应商 |
| `models *` | 模型与 Ollama |
| `skills *` | 技能开关 |
| `admin *` | 概览、审计、轮换 JWT |
| `captcha reset` | 清验证码配置 |
| `backup *` | 备份恢复 |
| `plugin *` | 插件 |
| `acp` | ACP 服务器 |
| `update` | 升级 |
| `clean` | 清理数据（危险） |
| `completion` | shell 补全 |

`octop clean --all` 会删除数据，必须 `--yes` 确认，先备份。

---

## 4. 配置说明

### 4.1 配置优先级

环境变量覆盖 `config.json` 字段；`~/.octop/env` 在启动时注入。向导写入的数据库配置与 `OCTOP_DATABASE_*` 交互时，以设计文档中的 `database_in_file` 语义为准：不要一边改 JSON 一边留着相反的环境变量。JSON 损坏不会被静默覆盖，启动会失败并提示配置文件损坏——这是保护，修好 JSON 再启动。

### 4.2 `OctopConfig` 主要字段

| 字段 | 环境变量 | 含义 |
|------|----------|------|
| bind_host | OCTOP_BIND_HOST | 监听地址，公网暴露用 0.0.0.0 并配防火墙 |
| port | OCTOP_PORT | HTTP 端口 |
| log_level | OCTOP_LOG_LEVEL | 日志级别 |
| access_token_ttl_seconds | OCTOP_ACCESS_TOKEN_TTL | JWT 有效期，默认一天量级 |
| login_max_attempts / login_lockout_seconds | OCTOP_LOGIN_MAX_ATTEMPTS / OCTOP_LOGIN_LOCKOUT_SECONDS | 锁定策略 |
| cors_origins | OCTOP_CORS_ORIGINS | 跨域；控制台分离部署时必填 |
| default_timezone | OCTOP_DEFAULT_TIMEZONE | 显示与 Cron 时区 |
| enable_dashboard | OCTOP_ENABLE_DASHBOARD | 是否挂载 SPA |
| enable_api_docs | OCTOP_ENABLE_API_DOCS | `/api/docs` Scalar，默认关 |
| history_v2_enabled | OCTOP_HISTORY_V2_ENABLED | 历史归档 |
| require_setup_password | OCTOP_REQUIRE_SETUP_PASSWORD | 向导口令 |
| max_upload_mb | OCTOP_MAX_UPLOAD_MB | 上传上限 1–1024 |
| browser_idle_timeout_minutes | OCTOP_BROWSER_IDLE_TIMEOUT_MINUTES | 浏览器会话空闲 |
| capabilities.mobile | OCTOP_ENABLE_MOBILE | 远程手机 |
| database.* | OCTOP_DATABASE_* | sqlite 或 postgresql |
| tls.* | （见 TLS 章） | 证书 |
| backup.* | OCTOP_BACKUP_* | 自动备份 |

### 4.3 数据库配置

默认 SQLite 文件相对 `OCTOP_HOME`。PostgreSQL 可设 URL 或 host/port/name/user/password。向导「测试数据库」失败时检查监听、账号、是否空库。备份与库类型绑定，不能把 PG 备份直接灌进 SQLite。

### 4.4 备份配置

`auto_enabled`、`schedule`、`retention_count`，以及 include 工作区/插件等开关。保留份数至少为 1。自动任务是系统 cron，不会出现在用户任务列表里随意删除。

### 4.5 TLS 配置

enabled、mode、domains、证书路径、ACME 邮箱。HTTP-01 需要 80 端口可达。域名与证书不一致会报 TLS_DOMAIN_MISMATCH。

### 4.6 首次安装环境变量

`OCTOP_ADMIN_USERNAME`、`OCTOP_DEFAULT_PASSWORD`（空则随机）、`OCTOP_ADMIN_DISPLAY_NAME`。LLM：`OPENAI_API_KEY`、`DASHSCOPE_API_KEY`。Langfuse：`OCTOP_LANGFUSE_ENABLED` 与三件套。验证码：`OCTOP_CAPTCHA_*`。

### 4.7 Agent 进程环境

运行中的 Agent 可见 `OCTOP_HOME`、`OCTOP_AGENT_ID`、`OCTOP_AUTH_DIR`、`OCTOP_SKILLS_DIR`。写技能脚本时可用这些变量定位文件，但不要假设路径字符串永远是 `~/.octop/...` 的 POSIX 写法（Windows 不同）。

### 4.8 控制台偏好

`GET/PATCH /api/preferences`：主题、布局、默认 Agent 等。这些存在用户 `preferences_json`，不进 config.json。

### 4.9 打开 API 文档

设置 `"enable_api_docs": true` 后访问 `/api/docs`。生产若暴露公网，请仅管理员可达或保持关闭，避免接口被枚举。

### 4.10 时区与语言的再强调

时区 ≠ 语言。改时区不会改界面文案。改语言不会改 Cron 触发时钟。两者都错的时候，用户会觉得「任务早了一小时而且按钮是英文」——请分别检查 `default_timezone` 与用户 locale。

---

## 5. 常见问题 100 个

### Q1. 安装脚本执行完找不到 octop 命令？

原因几乎总是当前 shell 没有加载新的 PATH。安装程序把可执行文件放在 `~/.octop/bin`。请新开终端，或执行 `source ~/.zshrc` / `source ~/.bashrc`。Windows 需重新打开 PowerShell。用 `echo $PATH` 确认该目录在内。若你用的是 fish 或其他 shell，需手动把该路径加入配置。不要用系统包管理器的同名软件覆盖。确认后执行 `octop version`，应打印 1.x 版本号。若提示权限不足，检查 `~/.octop/bin/octop` 的可执行位。开发者从源码运行则用 `uv run octop`。家庭用户不要把仓库里的 `src/` 直接当命令路径。若安装被公司代理阻断，使用 `--mirror` 指向可达的 PyPI 镜像后重试。安装日志若显示 uv 失败，先保证能访问 Python 下载源。完成这些检查后仍失败，删除不完整的 `~/.octop/venv` 再跑一次安装脚本，而不是混用系统 Python 硬装。

### Q2. 浏览器打开端口显示无法访问？

确认进程还在：`octop service status` 或前台 `octop run` 的终端没有退出。确认监听：config 里 bind_host 若为 127.0.0.1，则局域网其他设备无法访问，需改为 0.0.0.0 并重启。确认端口：`OCTOP_PORT` 与你在地址栏输入的一致，默认文档示例为 8088。云主机要在安全组放行该 TCP 端口。若启用了 TLS，应用 https 而不是 http。反向代理后要配置 WebSocket 升级，否则聊天会连上又断。公司电脑 HTTPS 解密代理可能打断 WSS。查看日志级别 info 是否有 listen 行。Docker 用户确认 `-p` 映射和卷。防火墙包括 Windows Defender 与 ufw。不要把 Dashboard 的 Vite 开发端口（如 5173）当成生产端口。

### Q3. 向导卡在数据库步骤？

SQLite 通常只要 `~/.octop` 可写。若改用 PostgreSQL，先在向导点「测试数据库」：失败则检查 URL、用户密码、是否允许连接、数据库是否已存在且为空。非空库会报 DATABASE_NOT_EMPTY，这是防止覆盖已有数据。权限不足时换成有 CREATEDB 的账号或预先建空库。SSL 模式与云厂商 PG 要求要匹配。测试成功后再保存。保存后若进程仍用旧 SQLite，检查是否残留 OCTOP_DATABASE_URL 指向别处。不要把生产库连接串贴到聊天给模型。迁移失败看日志中 migrate 堆栈，版本应到达 v15。损坏的半写入文件不要当备份。

### Q4. 忘记管理员密码怎么办？

若你还能用本机 CLI：`octop user passwd` 以文件系统权限重置，这是自托管的设计，没有云端「邮箱找回」。若你只有控制台且锁在外面，需要 SSH 到主机操作。重置后请立即登录并再改一次强口令。若账户是纯 SSO、password_hash 为空，应走 IdP 或先让 CLI 设口令。不要试图手改 SQLite 里的哈希除非你理解 Argon2id。重置属于敏感操作，审计日志会记录。若用户被 disabled，光改密码仍无法登录，需管理员取消禁用。锁定中的账户可在用户页解锁。最后一个管理员丢失且无 SSH 时，只能从备份恢复整个 `~/.octop`。

### Q5. 登录提示锁定或验证码失败？

多次错密会触发 LOGIN_LOCKED，等待 lockout 秒数或管理员解锁。验证码失败检查时钟、站点密钥、是否把 secret 配错环境。默认滑块只覆盖控制台。自动化脚本不要走滑块路径。清空验证码配置可用 `octop captcha reset` 后重启。浏览器插件拦截第三方脚本会导致强验证码无法加载。隐身窗口可排除扩展干扰。不要用多个标签同时打满失败次数。锁定是安全特性，降低后只应在内网短测时进行。

### Q6. JWT 经常过期被踢出？

默认 TTL 约 24 小时量级，可调 `access_token_ttl_seconds`。控制台会在剩余时间不足三分之一时用响应头刷新令牌；若反向代理丢掉自定义头 `X-Octop-Access-Token`，滑动续期失效。管理员若执行了 rotate-jwt-secret，所有人必须重新登录，这是预期。系统时间回拨会造成 TOKEN_EXPIRED。多标签页一个退出不影响服务端无状态 JWT，直到过期。不要把 token 存进公开 gist。移动浏览器后台冻结可能导致过期，重新登录即可。

### Q7. 对话窗口一直转圈没有回复？

先看 Agent 是否 running。再看供应商：key 无效、余额不足、base_url 填成网页而不是 API。看浏览器开发者工具 WS 是否 101。反向代理缓冲会让流式变成「突然一整段」，应关闭对 `/api/agents` WS 的缓冲。模型超时在日志中体现为上游错误。HITL 开启时可能只是在等你点批准。上下文过长可 `/compact` 或 `/new`。附件过大先压缩。知识库索引中不要同时重任务压满 CPU。重启 Agent 而不是整机作为第一步。

### Q8. 提示 AGENT_NOT_RUNNING 或 AGENT_FAILED？

未运行就 start。failed 要读 last_error：常见是 workspace 后端探测失败、MCP 初始化失败、磁盘满、权限不能写 `~/.octop/agents/<id>`。修复后 stop 再 start，或 reload。不要删除 failed Agent 除非你接受丢失工作区。共享磁盘延迟会导致启动超时，可改本地盘。Windows 下杀毒软件锁文件也会失败。查看审计与 metrics。多 Agent 同时 start 会占内存，逐个启动。

### Q9. 如何切换模型而不改代码？

对话里 `/model`，或线程的模型偏好，或 Agent default_model，或管理员全局 active-model。优先级以当前产品界面提示为准，一般线程覆盖 Agent 默认。改完发一条短消息验证。Ollama 模型名必须与 `ollama list` 一致。供应商未启用时列表里看不到。不要把「展示名」和 API 模型 id 搞混。Codex/ChatGPT OAuth 的模型列表取决于登录状态。

### Q10. 斜杠命令没有出现在飞书里？

斜杠目录按 origin 过滤，有些仅 ui/cli。飞书还需要机器人能读消息与回复。群聊可能要 @机器人。命令拼写错误返回无法识别。中文逗号、全角斜杠不行，必须英文 `/help`。网关未 boot 时 IM 整条链路不可用，看服务日志 ChannelManager。权限给的是应用而不是个人账号。重新保存通道配置后测试。

### Q11. 上传文件失败提示太大？

附件与知识库文档分别受 `max_upload_mb` 与知识库文档限额约束。压缩、拆分或提高配置后重启。类型不在白名单会 ATTACHMENT_UNSUPPORTED_TYPE。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q12. 知识库一直 pending？

看 embedding 是否配置成功；ONNX 是否下完；OCR extra 是否安装；磁盘；重启后续跑 resume_pending_index_jobs。错误状态点开 error_message。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q13. 检索不到我刚上传的 PDF？

先确认 indexed；扫描件需 OCR；分块过碎或过粗都会差；模型 embedding 维度与库创建时不一致需 reindex；Agent 未绑定该库。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q14. 连接器 OAuth 回调 400？

需要 HTTPS 公网 URL 与注册的 redirect 一致；时钟偏差；state 过期；重复用同一 callback。本地可用隧道，不要用 http://localhost 对要求 https 的提供商。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q15. 自定义 MCP 连不上？

检查命令是否在 PATH；Windows 要用对应可执行文件；环境变量脱敏后仍需真实值在服务器侧；看 CONNECTOR_MCP_LOAD_FAILED 日志指纹。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q16. Cron 不触发？

时区；enabled；表达式五段；服务是否在跑；task 失败看 last_error；系统休眠的笔记本不会触发。用 run-now 区分「调度问题」还是「Agent 问题」。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q17. 为什么任务早了一个小时？

夏令时或服务器时区与心理时区不同。改 default_timezone 并重启。不要用浏览器本地时间理解 Cron。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q18. 如何让家人各自有 Agent？

管理员邀请用户，每人创建自己的 Agent。不要共享同一个登录。可用共享专家模板减少重复配置。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q19. SSO 绑定失败提示身份被占用？

该 IdP subject 已绑其他本地用户。解绑或用那个用户登录。不要手工改库破坏 UNIQUE。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q20. 解绑 SSO 被拒绝？

纯 SSO 用户必须先设口令，否则无法登录。错误码 SSO_UNBIND_REQUIRES_PASSWORD。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q21. 如何限制某用户不能用终端工具？

安全策略工具黑名单 + 用户 permissions + 不要给其 Agent 开危险插件。HITL 可让管理员审批。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q22. 命令护栏误杀正常命令？

导出 YAML，收紧正则，避免过宽 `.*rm.*`。可重置出厂后逐步加规则。先在测试 Agent 验证。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q23. PII 脱敏后模型看不懂怎么办？

脱敏是保护，可对内网关闭部分规则，但不要在家用电脑对不可信通道关闭。日志同样脱敏。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q24. Token 突然不能对话？

TOKEN_QUOTA_EXCEEDED：管理员调高 user_policies 或等周期重置（若你实现了周期）。查看用量页定位哪个 Agent 烧得快。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q25. usage 里 cache 字段是什么？

提示缓存读写分开记账，便于分析前缀缓存。不是账单的唯一依据，以供应商控制台为准。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q26. 如何备份才能换机器？

停服务，backup create，复制备份与如需的工作区。新机器同 driver 恢复。密钥、JWT secret 都在库里，恢复后旧 token 仍有效除非你轮换。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q27. 备份报 schema 不兼容？

跨大版本恢复不被允许。应先升级旧实例到相同版本再备份，或按 CHANGELOG 逐步迁。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q28. SQLite 备份能恢复到 PostgreSQL 吗？

不能直接。BACKUP_DRIVER_MISMATCH。需要导出/迁移工具或按官方迁移指南，而不是改扩展名。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q29. Let's Encrypt 失败？

域名 A 记录、80 端口、防火墙、是否多人同时 issue。看 preflight。通配符要 DNS-01，当前以 HTTP-01 为主。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q30. 开启 API 文档安全吗？

内网可以。公网建议关。文档会暴露路由形状，虽仍要 JWT，但增加攻击面认知。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q31. CORS 错误？

把控制台 origin 加入 cors_origins，含协议和端口，重启。不要用 `*` 还期望带 cookie/头的复杂场景。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q32. Windows 路径报错？

不要在测试或配置里写死 `/home/...`。工作区路径用界面选择。开发贡献遵循 posix_only。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q33. macOS 远程桌面黑屏？

授予屏幕录制与辅助功能，重新安装桌面组件，检查会话上限。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q34. Linux 无桌面如何远程？

使用产品提供的一键隔离桌面脚本，不要在 SSH 里期待 GUI，除非装了虚拟帧缓冲。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q35. 手机画面卡顿？

ADB 编码、网络、USB 质量。模拟器比真机更卡。降分辨率。确认 mobile 能力已 probe。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q36. 浏览器自动化打不开站？

Playwright 内核未装、沙箱、公司 SSL 中间人。headless 被反爬拦截需有头会话。空闲太短被杀。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q37. 终端里执行 sudo 没反应？

无 TTY 交互；护栏可能禁止；不要把密码交给模型。用本机终端完成提权操作。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q38. ACP 在 IDE 里连不上？

确认 `octop acp --agent` 在前台、Agent running、IDE 配的是 stdio 不是错端口。看 --debug。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q39. 出站 Claude Code 不跑？

runner 可执行文件路径、登录状态、HITL 拒绝、工作区路径。先在系统 shell 手动跑 runner。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q40. 专家创建的 Agent 没有技能？

模板技能需启动后同步。reload。远程 backend 可能跳过部分播种。看启动日志 bootstrap。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q41. 如何从已有 Agent 发布专家？

控制台发布专家，slug 唯一。注意不要把密钥写进 SOUL.md。接收方 from-expert 创建。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q42. SkillHub 安装失败 SSL？

公司根证书、时间、代理。错误 SKILLHUB_SSL_FAILED。可改用导入 URL 或离线 ZIP。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q43. 插件安装提示已存在？

先卸载。id 冲突。不要改目录名绕过。只装签名或你审计过的源。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q44. 天气插件没出现？

bundled 默认关；管理员启用；Agent 打开该工具；网络出站允许天气 API。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q45. 记忆 migrate 后丢失？

只拷了 sqlite 没拷工作区，或 PG schema 名变了。用 portable doctor。history v2 与 PG 组合有约束。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q46. 多 Agent 互相 @ 死循环？

看 mailbox 文档；限制谁能 call_agent；不要在两个 IM 通道交叉转发同一消息。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q47. Dashboard 静态资源 404？

要用构建后的 SPA；开发模式走 Vite。assets 路径 404 不会回退 index，避免 JS MIME 错误。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q48. 改了 i18n 一半中文一半英文？

前后端键必须成对。贡献者跑 `uv run pytest tests/unit/i18n -q`。用户侧清缓存。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q49. 如何完全卸载？

停服务，`octop clean --all --yes` 或删 `~/.octop`，再删 PATH 行。Docker 删卷。不可逆。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q50. 日志太多？

调 log_level。不要在生产长期 debug。轮转用系统 journald/logrotate。不要把日志提交 git。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q51. 如何只暴露局域网？

bind 0.0.0.0 + 防火墙只放局域网段，或 bind 内网 IP。再加 TLS 与强口令。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q52. 能否多 worker 提高并发？

设计是单进程 workers=1。加机器请水平部署独立 OCTOP_HOME，而不是多 worker 抢同一 SQLite。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q53. SQLite 锁等待？

WAL 已开。避免外挂进程长时间写同一文件。备份时用产品 backup 而不是拷正在写的 db。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q54. 如何看审计谁删了用户？

admin 审计日志按 action/target 过滤。CLI `octop admin audit`。定期导出。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q55. 邀请码被人抢注？

设过期、用完即废、不要发到公开群。撤销未使用码。兑换有频率限制。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q56. 邮箱重复无法创建？

EMAIL_TAKEN。邮箱可空但非空则部分唯一。先改旧用户邮箱。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q57. 用户名规则？

唯一即可，登录身份是 username。不要用可猜测的 admin 以外的弱名当唯一管理员。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q58. 如何只读某个知识库给同事？

共享开关与可见性以当前 ACL 为准；不要把文件拷到聊天。更细的 ACL 见设计文档演进。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q59. 文档是扫描件？

安装 knowledge-ocr extra，选 OCR 后端。识别质量依赖图像清晰度。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q60. ONNX 下载中断？

看 download-status；重试；磁盘；Hugging Face 网络。下完再 activate、test。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q61. embedding 用哪个模型？

中文语料选支持中文的模型；维度在建库时固定。中途更换必须全库 reindex。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q62. 能否用 pgvector？

当前知识索引默认 sidecar SQLite。PG 主要用于控制平面。不要自行改表当向量库除非你能维护分叉。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q63. 连接器共享给全员安全吗？

shared 实例等于共享凭据能力。只共享只读类新闻，不共享邮箱。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q64. QQ 邮箱要什么密码？

IMAP 应用专用密码，不是登录密码。开启 IMAP。网络需能访问邮件服务器。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q65. 飞书机器人重复回复？

加密策略、事件订阅重复、两个通道同应用。只保留一个 enabled 通道。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q66. 钉钉企业内部应用权限？

按官方文档开消息读权限。测试先私聊。群机器人与企业内部应用配置不同。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q67. Telegram 默认英文？

通道类型提示 locale 为 en。用户偏好可覆盖。这不是 bug。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q68. Discord 在列表里看不到？

部分 kind 在类型里存在但主 UI 未启用。以当前控制台通道常量列表为准。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q69. 如何让 Cron 调用连接器？

任务上填 mcp_servers 或依赖 Agent 已绑定的连接器。text 类型不会调模型。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q70. fresh_thread 有什么用？

每次运行新线程，避免旧对话污染。日报类建议打开。连续项目可关掉。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q71. prompt 最长？

API 层约 2000 字符。更长请放到技能或知识库，让任务 prompt 变短。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q72. 如何暂停所有任务？

停服务或把任务 enabled 关掉。系统备份/TLS 任务不在用户列表，需改 config。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q73. 主动关怀太吵？

关 enabled 或缩小 active hours、增大 interval。检查是否多 session 重复（设计有防重）。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q74. 如何关闭 Dashboard 只留 API？

enable_dashboard false。仍要保护 JWT。适合把 UI 另处托管的高级部署。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q75. CLI 与控制台对话是否同一线程？

默认 session_key 不同（dashboard vs cli）。要续聊需指定同一 session/thread id。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q76. 如何用脚本发消息？

embedded `octop chats send` 或 HTTP+JWT。不要解析 HTML。注意 Agent running。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q77. WS 与旧 SSE？

流式聊天已是 WS；HITL resume 仍 SSE。客户端不要混用。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q78. 轨迹导出给谁看？

排障与审计。可能含工具输入，导出前脱敏。不要公开。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q79. 如何轮换供应商密钥？

在 admin providers 更新 api_key，reload Agent。旧 key 在供应商侧作废。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q80. 本地供应商为什么删不掉？

PROVIDER_LOCAL_PROTECTED。这是保护 Ollama 一类入口。可 disable。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q81. fetch-models 为空？

base_url 需到 /v1 这一层视实现而定；鉴权头；上游禁用列表接口。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q82. 语音识别不准？

换 STT 供应商或浏览器实现。采样率、噪音。TTS 用 edge-tts 等配置。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q83. 媒体生成没入口？

管理员配置媒体模型。配额与审核由上游决定。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q84. Langfuse 连不上？

开关、三件套 URL、网络。test 接口。失败不影响主对话但无观测。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q85. metrics 在哪看？

admin overview / metrics 端点。是进程内计数，重启清零，不是时序数据库。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q86. 如何升级到新版本？

octop update --check 再 upgrade。看 CHANGELOG。先备份。前端资源随 wheel。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q87. 升级后白屏？

强刷缓存；确认不是旧 SPA 对新技术接口。看 console MIME。重新 build-frontend 仅开发者。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q88. Docker 升级丢数据？

没挂卷。务必持久化 OCTOP_HOME。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q89. 如何改端口避免冲突？

OCTOP_PORT 或 run --port，并改反向代理。用户书签要更新。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q90. 多网卡绑哪个？

bind_host 指定。防火墙同步。TLS 域名解析到正确网卡。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q91. 能否用外部 Nginx 终结 TLS？

可以，后端 HTTP 仅内网。注意 WebSocket headers。ACME 可在 Nginx 侧做。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q92. setup 密码忘了？

require_setup_password 场景下用环境或文件。完成向导后该锁打开。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q93. init --force 危险吗？

会覆盖初始化假设，可能毁数据。只在空环境或你明确要重置时用。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q94. clean --dry-run 是什么？

只打印将删内容。务必先看再 --yes。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q95. completion 如何安装？

octop completion install bash|zsh。新开终端。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q96. JSON 输出给脚本？

全局 --json。解析错误码字段而不是英文句子。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q97. 权限目录 API 是什么？

GET /api/users/permissions 给管理界面渲染可选项，不是用户自己提权。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q98. workspace_root 策略？

限制 Agent 主机目录。容器内另有限制。设错会导致工具全失败。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q99. Windows 服务怎么装？

见 service 命令与文档；权限用专用用户。不要用 Administrator 日常跑。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。

### Q100. NAS 上跑卡顿？

把数据库放 SSD；知识库 embedding 很吃 CPU；减少并发浏览器会话。 处理时请按「先看界面报错码 → 再看 Agent 状态 → 再看服务日志 → 最后改配置重启」的顺序，避免同时改端口、时区、数据库三项导致无法归因。Octop 是单进程自托管系统，大多数「偶发失败」其实是上游 LLM、磁盘满、权限或反向代理缓冲。改动前备份 `~/.octop`，改动后用一条最短对话或 `octop agent list` 验证。若错误码能在 `1DESIGN.md` 的 ErrorCode 表对上，优先信错误码而不是猜测。家庭场景建议每个真人一个用户；团队场景用邀请码而不是共享管理员。涉及密钥的截图发送前请打码。仍失败时记录：操作系统、Octop 版本、是否 Docker、数据库类型、发生时间（服务器时区）。


---

## 6. 故障排查流程（建议打印）

1. 记录版本：`octop version`。  
2. 记录时间（服务器时区）与用户名、Agent id。  
3. 复制错误信封中的 `code` 字段。  
4. 查本手册第五章与 `1DESIGN.md` 错误码。  
5. 检查 Agent last_state、磁盘、网络、供应商控制台。  
6. 重启 Agent，仍不行再重启服务。  
7. 从备份恢复是最后手段。  

---

## 7. 安全使用建议

使用强口令；公网必须 TLS；不要把管理员给所有家人；插件与 MCP 视同代码执行；HITL 对生产 shell 打开；定期备份并在隔离目录试恢复；轮换已泄漏的供应商密钥与 JWT secret。

---

## 8. 手册结束语

Octop 把「家里的助手」和「小团队的专家团」做成同一套软件。先让对话稳定，再加通道与知识库，最后才上桌面控制与 ACP。配置项虽多，但日常只需记住：服务在跑、Agent 在跑、模型能测通、备份存在。更多架构理由读 `1DESIGN.md`，未来演进读 `3SOLUTION.md`。

## 附录使用场景 1

本附录给出第 1 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 2

本附录给出第 2 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 3

本附录给出第 3 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 4

本附录给出第 4 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 5

本附录给出第 5 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 6

本附录给出第 6 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 7

本附录给出第 7 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 8

本附录给出第 8 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 9

本附录给出第 9 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 10

本附录给出第 10 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 11

本附录给出第 11 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 12

本附录给出第 12 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 13

本附录给出第 13 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 14

本附录给出第 14 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 15

本附录给出第 15 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 16

本附录给出第 16 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 17

本附录给出第 17 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 18

本附录给出第 18 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 19

本附录给出第 19 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 20

本附录给出第 20 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 21

本附录给出第 21 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 22

本附录给出第 22 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 23

本附录给出第 23 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 24

本附录给出第 24 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 25

本附录给出第 25 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 26

本附录给出第 26 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 27

本附录给出第 27 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 28

本附录给出第 28 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 29

本附录给出第 29 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 30

本附录给出第 30 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 31

本附录给出第 31 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 32

本附录给出第 32 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 33

本附录给出第 33 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 34

本附录给出第 34 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 35

本附录给出第 35 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 36

本附录给出第 36 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 37

本附录给出第 37 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 38

本附录给出第 38 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 39

本附录给出第 39 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 40

本附录给出第 40 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 41

本附录给出第 41 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 42

本附录给出第 42 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 43

本附录给出第 43 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 44

本附录给出第 44 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 45

本附录给出第 45 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 46

本附录给出第 46 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 47

本附录给出第 47 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 48

本附录给出第 48 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 49

本附录给出第 49 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 50

本附录给出第 50 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 51

本附录给出第 51 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 52

本附录给出第 52 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 53

本附录给出第 53 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 54

本附录给出第 54 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 55

本附录给出第 55 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 56

本附录给出第 56 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 57

本附录给出第 57 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 58

本附录给出第 58 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 59

本附录给出第 59 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 60

本附录给出第 60 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 61

本附录给出第 61 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 62

本附录给出第 62 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 63

本附录给出第 63 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 64

本附录给出第 64 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 65

本附录给出第 65 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 66

本附录给出第 66 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 67

本附录给出第 67 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 68

本附录给出第 68 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 69

本附录给出第 69 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 70

本附录给出第 70 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 71

本附录给出第 71 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 72

本附录给出第 72 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 73

本附录给出第 73 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 74

本附录给出第 74 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 75

本附录给出第 75 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 76

本附录给出第 76 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 77

本附录给出第 77 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 78

本附录给出第 78 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。
## 附录使用场景 79

本附录给出第 79 类真实使用场景的逐步说明。场景假设你已经完成安装、向导与模型测试。请在控制台选择对应 Agent，确认其状态为运行中，然后按界面标签进入功能页。若该功能需要管理员权限，请先用管理员账号登录或请管理员代为打开开关。操作完成后，用一条简短对话验证 Agent 确实感知到了新配置，例如询问「你现在能访问哪些知识库」。不要在未备份的情况下批量删除线程、技能或用户。时区以服务器为准，语言以用户偏好为准，两者独立。附件一律走工作区 inbound，不要指望系统剪贴板在服务器上可用。IM 通道验证先私聊后群聊，避免机器人在群里刷屏。Cron 先用 run-now，再交给调度器。连接器先测连通，再在对话中点名让 Agent 调用。出现 OctopError 时把 code 记下来，不要只截图气泡。本场景与第三章功能说明互补：第三章按功能横切，附录按任务纵切。家庭用户可跳过 ACP 与远程手机；开发者可跳过育儿专家模板。请始终使用官方安装路径与官方 CLI，避免混合 pip 全局包。完成场景后，如果效果不稳定，优先 `/compact` 或新开线程，而不是反复改系统提示。系统提示应短，细节放技能与知识库。多 Agent 时给每个 Agent 起能分清职责的中文名。共享连接器等于共享权限，请谨慎。定期打开用量页，防止某个定时任务在夜间耗尽配额。升级前读 CHANGELOG，升级后抽查对话、任务、备份。本段重复强调是为了让手册达到可独立交付的厚度，并覆盖培训现场需要的口语化步骤。