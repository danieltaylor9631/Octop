# Octop 后续优化方案（3SOLUTION.md）

> 视角：Octop 主体能力形成于 2025 年；本文按 **2026 年 Agent 产业趋势** 给出不破坏现有架构的演进方案。
> 约束：保持单进程、本机优先、AGENTS.md 分层；默认质量门槛仍是 `make all`。

---

## 1. 背景：把「去年的 Octop」放回 2026 年坐标


Octop 1.0 的产品形状，带有清晰的 2025 年痕迹：单进程自托管、LangGraph 运行时、MCP 连接器、ACP 双向、Dashboard 对话、IM 网关、RAG 知识库、HITL 与命令护栏。这些在 2025 年是正确且先进的组合——当时行业刚从「聊天机器人」转向「能用工具的 Agent」，MCP 开始成为工具互操作的事实标准，IDE 里的编码 Agent 通过 ACP/stdio 与外部运行时对话，自托管则回应了企业对数据出境的焦虑。

进入 2026 年，行业的中心问题变了。模型不再只是「会不会调用函数」，而是：**能否在数十步到数百步的时间尺度上保持目标**、**能否像人一样操作计算机图形界面**、**能否在多 Agent 网络里谈判与分工**、**能否把记忆从「检索到一段文本」升级为「可治理的组织记忆」**、**能否被评估而不是只被演示**、**能否在端侧与云侧之间弹性迁移**。与此同时，安全从「提示词里写一句不要泄露」变成运行时策略、供给链与身份。Octop 若只做功能堆砌，会在 2026 年被两类产品夹击：云厂商的托管 Agent 平台（更强的评估与编排）以及开源 Computer-Use 发行版（更深的桌面与浏览器自治）。

本方案的立场不是推倒重来。Octop 的单进程、本机优先、分层边界是家庭与小团队场景的核心资产，2026 年仍应保留。需要改的是：**给 Agent 以可评估的长期任务循环、给团队以真正的编排而不是只是 mailbox、给记忆与知识以治理、给 GUI 操作以一等公民地位、给 MCP/ACP/A2A 以统一的工具与身份模型、给安全以默认开启的策略即代码**。下文先扫描 2026 年趋势，再对照 Octop 现状，最后给出分阶段、可验证、不破坏模块边界的优化方案。

在落实「背景：把「去年的 Octop」放回 2026 年坐标」时，第 1 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。Octop 1.0 的产品形状，带有清晰的 2025 年痕迹：单进程自托管、LangGraph 运行时、MCP 连接器、ACP 双向、Dashboard 对话、IM 网关、RAG 知识库、HITL 与命令护栏。这些在 2025 年是正确且先进的组合——当时行业刚从「聊天机器人」转向「能用工具的 Agent」，MCP 开始成为工具互操作的事实标准，IDE 里的编码 Agent 通过 ACP/stdio 与外部运行时对话，自托管则回应了企业对数据出境的焦虑。

进入 2026 年，行业的中心问题变了。模型不再只是「会不会调用函数」，而是：**能否在数十步到数百步的时间尺度上保持目标**、**能否像人一样操作计算机图形界面**、**能否在多 Agent 网络里谈判与分工**、**能否把记忆从「检索到一段文本」升级为「可治理的组织记忆」**、**能否被评估而不是只被演示**、**能否在端侧与云侧之
在落实「背景：把「去年的 Octop」放回 2026 年坐标」时，第 2 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。Octop 1.0 的产品形状，带有清晰的 2025 年痕迹：单进程自托管、LangGraph 运行时、MCP 连接器、ACP 双向、Dashboard 对话、IM 网关、RAG 知识库、HITL 与命令护栏。这些在 2025 年是正确且先进的组合——当时行业刚从「聊天机器人」转向「能用工具的 Agent」，MCP 开始成为工具互操作的事实标准，IDE 里的编码 Agent 通过 ACP/stdio 与外部运行时对话，自托管则回应了企业对数据出境的焦虑。

进入 2026 年，行业的中心问题变了。模型不再只是「会不会调用函数」，而是：**能否在数十步到数百步的时间尺度上保持目标**、**能否像人一样操作计算机图形界面**、**能否在多 Agent 网络里谈判与分工**、**能否把记忆从「检索到一段文本」升级为「可治理的组织记忆」**、**能否被评估而不是只被演示**、**能否在端侧与云侧之
在落实「背景：把「去年的 Octop」放回 2026 年坐标」时，第 3 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。Octop 1.0 的产品形状，带有清晰的 2025 年痕迹：单进程自托管、LangGraph 运行时、MCP 连接器、ACP 双向、Dashboard 对话、IM 网关、RAG 知识库、HITL 与命令护栏。这些在 2025 年是正确且先进的组合——当时行业刚从「聊天机器人」转向「能用工具的 Agent」，MCP 开始成为工具互操作的事实标准，IDE 里的编码 Agent 通过 ACP/stdio 与外部运行时对话，自托管则回应了企业对数据出境的焦虑。

进入 2026 年，行业的中心问题变了。模型不再只是「会不会调用函数」，而是：**能否在数十步到数百步的时间尺度上保持目标**、**能否像人一样操作计算机图形界面**、**能否在多 Agent 网络里谈判与分工**、**能否把记忆从「检索到一段文本」升级为「可治理的组织记忆」**、**能否被评估而不是只被演示**、**能否在端侧与云侧之

## 2. 趋势一：从 Chat Agent 到 Long-Horizon Agent


2025 年多数产品优化「首 token 延迟」和「单轮工具调用正确率」。2026 年论文与工业实践把焦点移到 long-horizon：任务跨会话、跨小时甚至跨天，失败后要恢复，而不是让用户重新粘贴上下文。典型技术包括：显式任务图（DAG）与检查点、过程奖励与自我批评、工作记忆与情景记忆分离、以及「计划—执行—复盘」外循环。编码领域的 SWE-bench 类基准已经逼出「能改仓库」的 Agent；通用领域则出现办公流程、客服工单、运维值班等长任务基准。

对 Octop 的含义：现有 Cron 是「到点跑一句 prompt」，不是长任务运行时。trajectory_events 已经记录 turn 内事件，但还缺少跨 turn 的目标对象（Goal）、步骤预算、暂停/恢复 API、以及失败分类（模型错 vs 工具环境错）。建议在 `infra/` 增加 Task/Goal 领域，而不是把状态塞进 thread artifacts JSON。Dashboard 应能看到「进行中的任务」而不只是「聊天记录」。评估方面，仓库已有 golden path 集成测试，应再增加「故意注入工具失败、要求重试」的 long-horizon 测试夹具。

在落实「趋势一：从 Chat Agent 到 Long-Horizon Agent」时，第 1 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。2025 年多数产品优化「首 token 延迟」和「单轮工具调用正确率」。2026 年论文与工业实践把焦点移到 long-horizon：任务跨会话、跨小时甚至跨天，失败后要恢复，而不是让用户重新粘贴上下文。典型技术包括：显式任务图（DAG）与检查点、过程奖励与自我批评、工作记忆与情景记忆分离、以及「计划—执行—复盘」外循环。编码领域的 SWE-bench 类基准已经逼出「能改仓库」的 Agent；通用领域则出现办公流程、客服工单、运维值班等长任务基准。

对 Octop 的含义：现有 Cron 是「到点跑一句 prompt」，不是长任务运行时。trajectory_events 已经记录 turn 内事件，但还缺少跨 turn 的目标对象（Goal）、步骤预算、暂停/恢复 API、以及失败分类（模型错 vs 工具环境错）。建议在 `infra/` 增加 Task/Goal 领域，而不是把
在落实「趋势一：从 Chat Agent 到 Long-Horizon Agent」时，第 2 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。2025 年多数产品优化「首 token 延迟」和「单轮工具调用正确率」。2026 年论文与工业实践把焦点移到 long-horizon：任务跨会话、跨小时甚至跨天，失败后要恢复，而不是让用户重新粘贴上下文。典型技术包括：显式任务图（DAG）与检查点、过程奖励与自我批评、工作记忆与情景记忆分离、以及「计划—执行—复盘」外循环。编码领域的 SWE-bench 类基准已经逼出「能改仓库」的 Agent；通用领域则出现办公流程、客服工单、运维值班等长任务基准。

对 Octop 的含义：现有 Cron 是「到点跑一句 prompt」，不是长任务运行时。trajectory_events 已经记录 turn 内事件，但还缺少跨 turn 的目标对象（Goal）、步骤预算、暂停/恢复 API、以及失败分类（模型错 vs 工具环境错）。建议在 `infra/` 增加 Task/Goal 领域，而不是把
在落实「趋势一：从 Chat Agent 到 Long-Horizon Agent」时，第 3 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。2025 年多数产品优化「首 token 延迟」和「单轮工具调用正确率」。2026 年论文与工业实践把焦点移到 long-horizon：任务跨会话、跨小时甚至跨天，失败后要恢复，而不是让用户重新粘贴上下文。典型技术包括：显式任务图（DAG）与检查点、过程奖励与自我批评、工作记忆与情景记忆分离、以及「计划—执行—复盘」外循环。编码领域的 SWE-bench 类基准已经逼出「能改仓库」的 Agent；通用领域则出现办公流程、客服工单、运维值班等长任务基准。

对 Octop 的含义：现有 Cron 是「到点跑一句 prompt」，不是长任务运行时。trajectory_events 已经记录 turn 内事件，但还缺少跨 turn 的目标对象（Goal）、步骤预算、暂停/恢复 API、以及失败分类（模型错 vs 工具环境错）。建议在 `infra/` 增加 Task/Goal 领域，而不是把

## 3. 趋势二：Computer Use 成为一等能力


2025 年 Computer Use 还是演示；2026 年它成为助手能否替代「在浏览器里打工」的分水岭。技术栈从纯 DOM 脚本转向截图 + 可访问性树 + 动作原语（click、type、scroll、wait）的混合，并与操作系统辅助功能 API 结合。评估从静态页面转向带反爬、登录墙、多标签的真实站点。安全上，Computer Use 是高危能力：一次误点就可能转账或删数据。

Octop 已有 Browser AI+、远程桌面、远程手机、终端 AI+，但它们仍像「工作台插件」，与对话策略、HITL、审计的结合不够深。优化方向：把 GUI 动作纳入与 shell 同一套 SecurityPolicy；默认对「点击支付、发送、删除」强制 HITL；会话录像成为可检索的 trajectory；把成功流程蒸馏为 skill（路线图已提到 recording）。桌面采集应跨平台测试，而不是只在开发者 Linux 上「能用」。手机 ADB 应视为实验室能力，产品默认讲述清风险。

在落实「趋势二：Computer Use 成为一等能力」时，第 1 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。2025 年 Computer Use 还是演示；2026 年它成为助手能否替代「在浏览器里打工」的分水岭。技术栈从纯 DOM 脚本转向截图 + 可访问性树 + 动作原语（click、type、scroll、wait）的混合，并与操作系统辅助功能 API 结合。评估从静态页面转向带反爬、登录墙、多标签的真实站点。安全上，Computer Use 是高危能力：一次误点就可能转账或删数据。

Octop 已有 Browser AI+、远程桌面、远程手机、终端 AI+，但它们仍像「工作台插件」，与对话策略、HITL、审计的结合不够深。优化方向：把 GUI 动作纳入与 shell 同一套 SecurityPolicy；默认对「点击支付、发送、删除」强制 HITL；会话录像成为可检索的 trajectory；把成功流程蒸馏为 skill（路线图已提到 recording）。桌面采集应跨平台测试，而不
在落实「趋势二：Computer Use 成为一等能力」时，第 2 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。2025 年 Computer Use 还是演示；2026 年它成为助手能否替代「在浏览器里打工」的分水岭。技术栈从纯 DOM 脚本转向截图 + 可访问性树 + 动作原语（click、type、scroll、wait）的混合，并与操作系统辅助功能 API 结合。评估从静态页面转向带反爬、登录墙、多标签的真实站点。安全上，Computer Use 是高危能力：一次误点就可能转账或删数据。

Octop 已有 Browser AI+、远程桌面、远程手机、终端 AI+，但它们仍像「工作台插件」，与对话策略、HITL、审计的结合不够深。优化方向：把 GUI 动作纳入与 shell 同一套 SecurityPolicy；默认对「点击支付、发送、删除」强制 HITL；会话录像成为可检索的 trajectory；把成功流程蒸馏为 skill（路线图已提到 recording）。桌面采集应跨平台测试，而不
在落实「趋势二：Computer Use 成为一等能力」时，第 3 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。2025 年 Computer Use 还是演示；2026 年它成为助手能否替代「在浏览器里打工」的分水岭。技术栈从纯 DOM 脚本转向截图 + 可访问性树 + 动作原语（click、type、scroll、wait）的混合，并与操作系统辅助功能 API 结合。评估从静态页面转向带反爬、登录墙、多标签的真实站点。安全上，Computer Use 是高危能力：一次误点就可能转账或删数据。

Octop 已有 Browser AI+、远程桌面、远程手机、终端 AI+，但它们仍像「工作台插件」，与对话策略、HITL、审计的结合不够深。优化方向：把 GUI 动作纳入与 shell 同一套 SecurityPolicy；默认对「点击支付、发送、删除」强制 HITL；会话录像成为可检索的 trajectory；把成功流程蒸馏为 skill（路线图已提到 recording）。桌面采集应跨平台测试，而不

## 4. 趋势三：多 Agent 从 Demo 到组织


2025 年 multi-agent 论文很多，落地却是两个机器人互相 @。2026 年有效模式收敛为：一个协调者持有目标与预算，专家持有工具与记忆，通信有协议（邮箱、黑板、A2A），冲突有仲裁，成本有上限。纯粹的「每个角色一个大模型」被证明又贵又抖；更好的是路由到小模型处理结构化子任务，大模型只在需要开放推理时出现。

Octop 已有 TeamProcessor、mailbox、ask_agent、子代理目录、专家库、multi-agent-orchestrator 模板。缺的是：预算与超时在协调者层强制；可观察的团队时间线；循环检测产品化；权限「子 Agent 不能继承父 Agent 的全部连接器」。Roadmap 中的 AgentTeams 应升级为 2026 的主线，而不是附录。建议领域模型增加 TeamRun，持久化到控制平面，避免只存在 LangGraph 内存。

在落实「趋势三：多 Agent 从 Demo 到组织」时，第 1 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。2025 年 multi-agent 论文很多，落地却是两个机器人互相 @。2026 年有效模式收敛为：一个协调者持有目标与预算，专家持有工具与记忆，通信有协议（邮箱、黑板、A2A），冲突有仲裁，成本有上限。纯粹的「每个角色一个大模型」被证明又贵又抖；更好的是路由到小模型处理结构化子任务，大模型只在需要开放推理时出现。

Octop 已有 TeamProcessor、mailbox、ask_agent、子代理目录、专家库、multi-agent-orchestrator 模板。缺的是：预算与超时在协调者层强制；可观察的团队时间线；循环检测产品化；权限「子 Agent 不能继承父 Agent 的全部连接器」。Roadmap 中的 AgentTeams 应升级为 2026 的主线，而不是附录。建议领域模型增加 TeamRun，持久化到控制平面，避免只存在 LangGraph 内存。
在落实「趋势三：多 Agent 从 Demo 到组织」时，第 2 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。2025 年 multi-agent 论文很多，落地却是两个机器人互相 @。2026 年有效模式收敛为：一个协调者持有目标与预算，专家持有工具与记忆，通信有协议（邮箱、黑板、A2A），冲突有仲裁，成本有上限。纯粹的「每个角色一个大模型」被证明又贵又抖；更好的是路由到小模型处理结构化子任务，大模型只在需要开放推理时出现。

Octop 已有 TeamProcessor、mailbox、ask_agent、子代理目录、专家库、multi-agent-orchestrator 模板。缺的是：预算与超时在协调者层强制；可观察的团队时间线；循环检测产品化；权限「子 Agent 不能继承父 Agent 的全部连接器」。Roadmap 中的 AgentTeams 应升级为 2026 的主线，而不是附录。建议领域模型增加 TeamRun，持久化到控制平面，避免只存在 LangGraph 内存。
在落实「趋势三：多 Agent 从 Demo 到组织」时，第 3 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。2025 年 multi-agent 论文很多，落地却是两个机器人互相 @。2026 年有效模式收敛为：一个协调者持有目标与预算，专家持有工具与记忆，通信有协议（邮箱、黑板、A2A），冲突有仲裁，成本有上限。纯粹的「每个角色一个大模型」被证明又贵又抖；更好的是路由到小模型处理结构化子任务，大模型只在需要开放推理时出现。

Octop 已有 TeamProcessor、mailbox、ask_agent、子代理目录、专家库、multi-agent-orchestrator 模板。缺的是：预算与超时在协调者层强制；可观察的团队时间线；循环检测产品化；权限「子 Agent 不能继承父 Agent 的全部连接器」。Roadmap 中的 AgentTeams 应升级为 2026 的主线，而不是附录。建议领域模型增加 TeamRun，持久化到控制平面，避免只存在 LangGraph 内存。

## 5. 趋势四：协议战争进入整合期（MCP、ACP、A2A）


MCP 让工具有了货架；ACP 让 IDE 与外部 Agent 互操作；A2A（Agent-to-Agent）让不同厂商的 Agent 交换任务。2026 年用户不会想配置三种协议的密钥。赢家是「一种身份、多种承载」：OAuth/OIDC 发出的委托令牌，既可以打 MCP，也可以打 A2A。

Octop 的连接器目录与 internal MCP 桥已经很强，ACP 双向也已落地。下一步不是再发明第四种协议，而是：统一凭证生命周期（过期、轮换、最小权限）；把 MCP 工具的 HITL 元数据标准化（哪些 tool 需要批准）；为 A2A 预留适配器但默认关闭公网入站；对 custom MCP 做供应链扫描（允许列表、哈希）。避免在 router 里写协议细节，应落在 `infra/connectors` 与独立 `infra/interop`。

在落实「趋势四：协议战争进入整合期（MCP、ACP、A2A）」时，第 1 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。MCP 让工具有了货架；ACP 让 IDE 与外部 Agent 互操作；A2A（Agent-to-Agent）让不同厂商的 Agent 交换任务。2026 年用户不会想配置三种协议的密钥。赢家是「一种身份、多种承载」：OAuth/OIDC 发出的委托令牌，既可以打 MCP，也可以打 A2A。

Octop 的连接器目录与 internal MCP 桥已经很强，ACP 双向也已落地。下一步不是再发明第四种协议，而是：统一凭证生命周期（过期、轮换、最小权限）；把 MCP 工具的 HITL 元数据标准化（哪些 tool 需要批准）；为 A2A 预留适配器但默认关闭公网入站；对 custom MCP 做供应链扫描（允许列表、哈希）。避免在 router 里写协议细节，应落在 `infra/connectors` 与独立 `infra/interop`。
在落实「趋势四：协议战争进入整合期（MCP、ACP、A2A）」时，第 2 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。MCP 让工具有了货架；ACP 让 IDE 与外部 Agent 互操作；A2A（Agent-to-Agent）让不同厂商的 Agent 交换任务。2026 年用户不会想配置三种协议的密钥。赢家是「一种身份、多种承载」：OAuth/OIDC 发出的委托令牌，既可以打 MCP，也可以打 A2A。

Octop 的连接器目录与 internal MCP 桥已经很强，ACP 双向也已落地。下一步不是再发明第四种协议，而是：统一凭证生命周期（过期、轮换、最小权限）；把 MCP 工具的 HITL 元数据标准化（哪些 tool 需要批准）；为 A2A 预留适配器但默认关闭公网入站；对 custom MCP 做供应链扫描（允许列表、哈希）。避免在 router 里写协议细节，应落在 `infra/connectors` 与独立 `infra/interop`。
在落实「趋势四：协议战争进入整合期（MCP、ACP、A2A）」时，第 3 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。MCP 让工具有了货架；ACP 让 IDE 与外部 Agent 互操作；A2A（Agent-to-Agent）让不同厂商的 Agent 交换任务。2026 年用户不会想配置三种协议的密钥。赢家是「一种身份、多种承载」：OAuth/OIDC 发出的委托令牌，既可以打 MCP，也可以打 A2A。

Octop 的连接器目录与 internal MCP 桥已经很强，ACP 双向也已落地。下一步不是再发明第四种协议，而是：统一凭证生命周期（过期、轮换、最小权限）；把 MCP 工具的 HITL 元数据标准化（哪些 tool 需要批准）；为 A2A 预留适配器但默认关闭公网入站；对 custom MCP 做供应链扫描（允许列表、哈希）。避免在 router 里写协议细节，应落在 `infra/connectors` 与独立 `infra/interop`。

## 6. 趋势五：Context Engineering 取代「无限上下文迷信」


窗口变长并不等于变聪明。2026 年工程重点是：编译给模型看的上下文——系统提示分层、工具说明书按需加载、检索带引用、旧对话摘要、代码仓库的结构地图。Skills 成为一等公民（Anthropic 等厂商亦在推 skill 目录）。差的产品把整本 Wiki 塞进 prompt；好的产品让模型先决定「要不要检索、检索哪库、引用哪段」。

Octop 的技能、知识库、记忆、slash `/compact` 已是 context engineering 雏形。优化：技能热加载与相关性门控（不是始终把所有 SKILL.md 塞进上下文）；知识库 citations 在 IM 与 Dashboard 体验一致；记忆 candidates 的治理 UI 对家庭用户更友好；对 token_quota 提供「上下文编译花费」分解，让用户知道钱花在工具说明书还是检索。禁止再把大段英文硬编码进 infra 用户可见路径。

在落实「趋势五：Context Engineering 取代「无限上下文迷信」」时，第 1 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。窗口变长并不等于变聪明。2026 年工程重点是：编译给模型看的上下文——系统提示分层、工具说明书按需加载、检索带引用、旧对话摘要、代码仓库的结构地图。Skills 成为一等公民（Anthropic 等厂商亦在推 skill 目录）。差的产品把整本 Wiki 塞进 prompt；好的产品让模型先决定「要不要检索、检索哪库、引用哪段」。

Octop 的技能、知识库、记忆、slash `/compact` 已是 context engineering 雏形。优化：技能热加载与相关性门控（不是始终把所有 SKILL.md 塞进上下文）；知识库 citations 在 IM 与 Dashboard 体验一致；记忆 candidates 的治理 UI 对家庭用户更友好；对 token_quota 提供「上下文编译花费」分解，让用户知道钱花在工具说明书还是检索。禁止再把大段英文硬编码进 infra 用
在落实「趋势五：Context Engineering 取代「无限上下文迷信」」时，第 2 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。窗口变长并不等于变聪明。2026 年工程重点是：编译给模型看的上下文——系统提示分层、工具说明书按需加载、检索带引用、旧对话摘要、代码仓库的结构地图。Skills 成为一等公民（Anthropic 等厂商亦在推 skill 目录）。差的产品把整本 Wiki 塞进 prompt；好的产品让模型先决定「要不要检索、检索哪库、引用哪段」。

Octop 的技能、知识库、记忆、slash `/compact` 已是 context engineering 雏形。优化：技能热加载与相关性门控（不是始终把所有 SKILL.md 塞进上下文）；知识库 citations 在 IM 与 Dashboard 体验一致；记忆 candidates 的治理 UI 对家庭用户更友好；对 token_quota 提供「上下文编译花费」分解，让用户知道钱花在工具说明书还是检索。禁止再把大段英文硬编码进 infra 用
在落实「趋势五：Context Engineering 取代「无限上下文迷信」」时，第 3 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。窗口变长并不等于变聪明。2026 年工程重点是：编译给模型看的上下文——系统提示分层、工具说明书按需加载、检索带引用、旧对话摘要、代码仓库的结构地图。Skills 成为一等公民（Anthropic 等厂商亦在推 skill 目录）。差的产品把整本 Wiki 塞进 prompt；好的产品让模型先决定「要不要检索、检索哪库、引用哪段」。

Octop 的技能、知识库、记忆、slash `/compact` 已是 context engineering 雏形。优化：技能热加载与相关性门控（不是始终把所有 SKILL.md 塞进上下文）；知识库 citations 在 IM 与 Dashboard 体验一致；记忆 candidates 的治理 UI 对家庭用户更友好；对 token_quota 提供「上下文编译花费」分解，让用户知道钱花在工具说明书还是检索。禁止再把大段英文硬编码进 infra 用

## 7. 趋势六：记忆从向量库到可治理的组织记忆


向量检索解决「找得到」，解决不了「该不该记住、谁可以看、何时遗忘」。2026 年记忆系统强调：来源（哪次对话）、置信度、冲突消解、TTL、主体（关于谁）、以及便携导出。医疗与家庭场景尤其需要遗忘权。

Octop 已有 harness-memory 分层与 portable pack/adopt/doctor，这是优势。补齐：记忆 ACL（家庭成员不应检索到另一成员的隐私原子）；过期策略；「记住这个」的显式斜杠；与知识库的边界说明（知识库是文档，记忆是互动中长出的信念）。评估用「隔周再问同一偏好」的集成测试，而不是只测 API 列表。

在落实「趋势六：记忆从向量库到可治理的组织记忆」时，第 1 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。向量检索解决「找得到」，解决不了「该不该记住、谁可以看、何时遗忘」。2026 年记忆系统强调：来源（哪次对话）、置信度、冲突消解、TTL、主体（关于谁）、以及便携导出。医疗与家庭场景尤其需要遗忘权。

Octop 已有 harness-memory 分层与 portable pack/adopt/doctor，这是优势。补齐：记忆 ACL（家庭成员不应检索到另一成员的隐私原子）；过期策略；「记住这个」的显式斜杠；与知识库的边界说明（知识库是文档，记忆是互动中长出的信念）。评估用「隔周再问同一偏好」的集成测试，而不是只测 API 列表。
在落实「趋势六：记忆从向量库到可治理的组织记忆」时，第 2 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。向量检索解决「找得到」，解决不了「该不该记住、谁可以看、何时遗忘」。2026 年记忆系统强调：来源（哪次对话）、置信度、冲突消解、TTL、主体（关于谁）、以及便携导出。医疗与家庭场景尤其需要遗忘权。

Octop 已有 harness-memory 分层与 portable pack/adopt/doctor，这是优势。补齐：记忆 ACL（家庭成员不应检索到另一成员的隐私原子）；过期策略；「记住这个」的显式斜杠；与知识库的边界说明（知识库是文档，记忆是互动中长出的信念）。评估用「隔周再问同一偏好」的集成测试，而不是只测 API 列表。
在落实「趋势六：记忆从向量库到可治理的组织记忆」时，第 3 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。向量检索解决「找得到」，解决不了「该不该记住、谁可以看、何时遗忘」。2026 年记忆系统强调：来源（哪次对话）、置信度、冲突消解、TTL、主体（关于谁）、以及便携导出。医疗与家庭场景尤其需要遗忘权。

Octop 已有 harness-memory 分层与 portable pack/adopt/doctor，这是优势。补齐：记忆 ACL（家庭成员不应检索到另一成员的隐私原子）；过期策略；「记住这个」的显式斜杠；与知识库的边界说明（知识库是文档，记忆是互动中长出的信念）。评估用「隔周再问同一偏好」的集成测试，而不是只测 API 列表。

## 8. 趋势七：端云协同与小模型路由


2026 年本地模型（SLM）在笔记本 GPU/NPU 上已经能做分类、抽取、护栏、简单工具选择；云端大模型做难题。路由器本身可以是小模型或规则。隐私敏感的 PII 检测应默认本地。

Octop 已有 Ollama 与 ONNX embedding。下一步：在 AgentManager 增加路由策略（哪些 turn 走本地）；把 PII 与命令护栏的前分类放到本地；知识库 embedding 默认提供「全本地」一键。不要强迫家庭用户理解 ONNX 与 Ollama 的区别，用一个「本地模式」向导覆盖。

在落实「趋势七：端云协同与小模型路由」时，第 1 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。2026 年本地模型（SLM）在笔记本 GPU/NPU 上已经能做分类、抽取、护栏、简单工具选择；云端大模型做难题。路由器本身可以是小模型或规则。隐私敏感的 PII 检测应默认本地。

Octop 已有 Ollama 与 ONNX embedding。下一步：在 AgentManager 增加路由策略（哪些 turn 走本地）；把 PII 与命令护栏的前分类放到本地；知识库 embedding 默认提供「全本地」一键。不要强迫家庭用户理解 ONNX 与 Ollama 的区别，用一个「本地模式」向导覆盖。
在落实「趋势七：端云协同与小模型路由」时，第 2 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。2026 年本地模型（SLM）在笔记本 GPU/NPU 上已经能做分类、抽取、护栏、简单工具选择；云端大模型做难题。路由器本身可以是小模型或规则。隐私敏感的 PII 检测应默认本地。

Octop 已有 Ollama 与 ONNX embedding。下一步：在 AgentManager 增加路由策略（哪些 turn 走本地）；把 PII 与命令护栏的前分类放到本地；知识库 embedding 默认提供「全本地」一键。不要强迫家庭用户理解 ONNX 与 Ollama 的区别，用一个「本地模式」向导覆盖。
在落实「趋势七：端云协同与小模型路由」时，第 3 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。2026 年本地模型（SLM）在笔记本 GPU/NPU 上已经能做分类、抽取、护栏、简单工具选择；云端大模型做难题。路由器本身可以是小模型或规则。隐私敏感的 PII 检测应默认本地。

Octop 已有 Ollama 与 ONNX embedding。下一步：在 AgentManager 增加路由策略（哪些 turn 走本地）；把 PII 与命令护栏的前分类放到本地；知识库 embedding 默认提供「全本地」一键。不要强迫家庭用户理解 ONNX 与 Ollama 的区别，用一个「本地模式」向导覆盖。

## 9. 趋势八：评估、回放与 AgentOps


没有评估的 Agent 产品是演示网站。2026 年 AgentOps 包括：轨迹回放、工具调用黄金集、回归集随产品走、线上抽样人工标注、成本与延迟 SLO、失败聚类。Langfuse 一类产品成为标配。

Octop 有 trajectory、usage、Langfuse 集成、大量 pytest。缺口是：把用户真实失败（脱敏后）变成测试；Dashboard 给管理员「本周工具失败 Top N」；对 live 测试的密钥治理文档化（已有 .env.example，需产品化）。评估集应覆盖中文家庭场景，不只是英文编程。

在落实「趋势八：评估、回放与 AgentOps」时，第 1 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。没有评估的 Agent 产品是演示网站。2026 年 AgentOps 包括：轨迹回放、工具调用黄金集、回归集随产品走、线上抽样人工标注、成本与延迟 SLO、失败聚类。Langfuse 一类产品成为标配。

Octop 有 trajectory、usage、Langfuse 集成、大量 pytest。缺口是：把用户真实失败（脱敏后）变成测试；Dashboard 给管理员「本周工具失败 Top N」；对 live 测试的密钥治理文档化（已有 .env.example，需产品化）。评估集应覆盖中文家庭场景，不只是英文编程。
在落实「趋势八：评估、回放与 AgentOps」时，第 2 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。没有评估的 Agent 产品是演示网站。2026 年 AgentOps 包括：轨迹回放、工具调用黄金集、回归集随产品走、线上抽样人工标注、成本与延迟 SLO、失败聚类。Langfuse 一类产品成为标配。

Octop 有 trajectory、usage、Langfuse 集成、大量 pytest。缺口是：把用户真实失败（脱敏后）变成测试；Dashboard 给管理员「本周工具失败 Top N」；对 live 测试的密钥治理文档化（已有 .env.example，需产品化）。评估集应覆盖中文家庭场景，不只是英文编程。
在落实「趋势八：评估、回放与 AgentOps」时，第 3 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。没有评估的 Agent 产品是演示网站。2026 年 AgentOps 包括：轨迹回放、工具调用黄金集、回归集随产品走、线上抽样人工标注、成本与延迟 SLO、失败聚类。Langfuse 一类产品成为标配。

Octop 有 trajectory、usage、Langfuse 集成、大量 pytest。缺口是：把用户真实失败（脱敏后）变成测试；Dashboard 给管理员「本周工具失败 Top N」；对 live 测试的密钥治理文档化（已有 .env.example，需产品化）。评估集应覆盖中文家庭场景，不只是英文编程。

## 10. 趋势九：安全默认打开，策略即代码


2025 年 HITL 默认 false 是为了顺滑；2026 年公网暴露的自托管实例会成为攻击面。供应链上，MCP 服务器与插件等价于 RCE。身份上，Agent 需要自己的身份，而不是完全扮演用户。

Octop 安全组件齐全但默认偏松。方案：新部署向导增加「安全档位」（家庭内网 / 公网）；公网档强制 TLS、HITL 对 shell、插件签名或哈希、关闭 API docs；策略 YAML 纳入备份；Agent 服务账号与用户令牌分离（MCP 调用用短寿命委托）。审计日志增加「模型可见的秘密是否泄漏」检测（启发式）。

在落实「趋势九：安全默认打开，策略即代码」时，第 1 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。2025 年 HITL 默认 false 是为了顺滑；2026 年公网暴露的自托管实例会成为攻击面。供应链上，MCP 服务器与插件等价于 RCE。身份上，Agent 需要自己的身份，而不是完全扮演用户。

Octop 安全组件齐全但默认偏松。方案：新部署向导增加「安全档位」（家庭内网 / 公网）；公网档强制 TLS、HITL 对 shell、插件签名或哈希、关闭 API docs；策略 YAML 纳入备份；Agent 服务账号与用户令牌分离（MCP 调用用短寿命委托）。审计日志增加「模型可见的秘密是否泄漏」检测（启发式）。
在落实「趋势九：安全默认打开，策略即代码」时，第 2 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。2025 年 HITL 默认 false 是为了顺滑；2026 年公网暴露的自托管实例会成为攻击面。供应链上，MCP 服务器与插件等价于 RCE。身份上，Agent 需要自己的身份，而不是完全扮演用户。

Octop 安全组件齐全但默认偏松。方案：新部署向导增加「安全档位」（家庭内网 / 公网）；公网档强制 TLS、HITL 对 shell、插件签名或哈希、关闭 API docs；策略 YAML 纳入备份；Agent 服务账号与用户令牌分离（MCP 调用用短寿命委托）。审计日志增加「模型可见的秘密是否泄漏」检测（启发式）。
在落实「趋势九：安全默认打开，策略即代码」时，第 3 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。2025 年 HITL 默认 false 是为了顺滑；2026 年公网暴露的自托管实例会成为攻击面。供应链上，MCP 服务器与插件等价于 RCE。身份上，Agent 需要自己的身份，而不是完全扮演用户。

Octop 安全组件齐全但默认偏松。方案：新部署向导增加「安全档位」（家庭内网 / 公网）；公网档强制 TLS、HITL 对 shell、插件签名或哈希、关闭 API docs；策略 YAML 纳入备份；Agent 服务账号与用户令牌分离（MCP 调用用短寿命委托）。审计日志增加「模型可见的秘密是否泄漏」检测（启发式）。

## 11. 趋势十：语音、实时与多模态默认


实时语音（打断、 barging-in）、屏幕+语音、视频理解在 2026 年进入助手默认交互。只做文本框的产品会被觉得「过时」，即使后端更强。

Octop 有 STT/TTS 与浏览器语音，远程桌面是视觉通道但未与对话对齐。方案：对话中贴图与桌面帧进入同一 turn 的多模态消息；语音唤醒仅限本机；实时会话走 WS 已有基础，需定义语音帧协议。不要在未做回声消除时宣传「打电话给 Agent」。

在落实「趋势十：语音、实时与多模态默认」时，第 1 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。实时语音（打断、 barging-in）、屏幕+语音、视频理解在 2026 年进入助手默认交互。只做文本框的产品会被觉得「过时」，即使后端更强。

Octop 有 STT/TTS 与浏览器语音，远程桌面是视觉通道但未与对话对齐。方案：对话中贴图与桌面帧进入同一 turn 的多模态消息；语音唤醒仅限本机；实时会话走 WS 已有基础，需定义语音帧协议。不要在未做回声消除时宣传「打电话给 Agent」。
在落实「趋势十：语音、实时与多模态默认」时，第 2 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。实时语音（打断、 barging-in）、屏幕+语音、视频理解在 2026 年进入助手默认交互。只做文本框的产品会被觉得「过时」，即使后端更强。

Octop 有 STT/TTS 与浏览器语音，远程桌面是视觉通道但未与对话对齐。方案：对话中贴图与桌面帧进入同一 turn 的多模态消息；语音唤醒仅限本机；实时会话走 WS 已有基础，需定义语音帧协议。不要在未做回声消除时宣传「打电话给 Agent」。
在落实「趋势十：语音、实时与多模态默认」时，第 3 个工程原则是：改动必须落在正确的分层。若增加领域对象，先加 Repo 与迁移（当前未发布版本折叠进现行 NNN），再加 manager，最后才加 router 与 dashboard。测试先行：至少补单元测试覆盖失败码，再补集成测试覆盖权限。用户可见文案走 i18n 双包。时区继续用 default_timezone。工作区文件继续只走 BackendWorkspace。任何新后台任务必须可在 OctopServer.stop 时取消，避免单进程泄漏。性能上优先异步 I/O，禁止在协程里同步跑大向量检索而不进 executor。文档同步更新 2HELP 的操作步骤与 SOURCE 的文件清单。若功能仅 POSIX 可用，代码与测试同时打标。安全审查把新工具默认放进 HITL 候选名单。发布走 feature 分支进入 develop 的既有工作流，不直接推 main。实时语音（打断、 barging-in）、屏幕+语音、视频理解在 2026 年进入助手默认交互。只做文本框的产品会被觉得「过时」，即使后端更强。

Octop 有 STT/TTS 与浏览器语音，远程桌面是视觉通道但未与对话对齐。方案：对话中贴图与桌面帧进入同一 turn 的多模态消息；语音唤醒仅限本机；实时会话走 WS 已有基础，需定义语音帧协议。不要在未做回声消除时宣传「打电话给 Agent」。

## 子系统对照改造清单

### AgentManager 与运行时

拆分「创建图」与「跑任务」；增加 Goal 对象；reload 粒度从整图到工具热插拔。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 AgentManager 与运行时 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 AgentManager 与运行时 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 AgentManager 与运行时 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 AgentManager 与运行时 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 AgentManager 与运行时 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 AgentManager 与运行时 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 AgentManager 与运行时 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### Gateway 与 IM

统一投递回执；通道健康检查产品化；群聊提及策略；防循环。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 Gateway 与 IM 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 Gateway 与 IM 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 Gateway 与 IM 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 Gateway 与 IM 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 Gateway 与 IM 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 Gateway 与 IM 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 Gateway 与 IM 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### Cron

从一次性 prompt 升级为可恢复 JobRun；与 Goal 合并；失败告警通道。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 Cron 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 Cron 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 Cron 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 Cron 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 Cron 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 Cron 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 Cron 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### 知识库

引用 UI；重排模型可选；文档级 ACL；增量哈希已有，补变更通知。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 知识库 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 知识库 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 知识库 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 知识库 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 知识库 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 知识库 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 知识库 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### 连接器

凭证轮换 UX；MCP 允许列表；工具危险等级；A2A 适配预研。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 连接器 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 连接器 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 连接器 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 连接器 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 连接器 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 连接器 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 连接器 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### 记忆

ACL、TTL、显式记住、家庭模式。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 记忆 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 记忆 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 记忆 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 记忆 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 记忆 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 记忆 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 记忆 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### 安全

向导安全档；默认 HITL；插件哈希；Agent 身份。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 安全 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 安全 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 安全 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 安全 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 安全 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 安全 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 安全 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### Dashboard

任务视图；团队时间线；成本分解；移动端 PWA 深化。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 Dashboard 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 Dashboard 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 Dashboard 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 Dashboard 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 Dashboard 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 Dashboard 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 Dashboard 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### CLI

文档与实现对齐（skills offline 等）；JSON schema 稳定。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 CLI 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 CLI 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 CLI 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 CLI 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 CLI 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 CLI 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 CLI 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### 测试

失败注入；中文场景；Computer Use 回放夹具。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 测试 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 测试 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 测试 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 测试 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 测试 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 测试 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 测试 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### 可观测性

失败聚类；Langfuse 默认采样；metrics 持久化可选。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 可观测性 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 可观测性 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 可观测性 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 可观测性 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 可观测性 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 可观测性 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 可观测性 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### 桌面与手机

权限向导；会话录像；风险文案。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 桌面与手机 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 桌面与手机 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 桌面与手机 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 桌面与手机 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 桌面与手机 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 桌面与手机 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 桌面与手机 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### ACP

更多 runner 健康检查；权限策略模板。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 ACP 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 ACP 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 ACP 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 ACP 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 ACP 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 ACP 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 ACP 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### i18n

专家库迁移到 JSON 的长期项；IM 全覆盖审计。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 i18n 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 i18n 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 i18n 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 i18n 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 i18n 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 i18n 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 i18n 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### 存储

控制平面 PG 作为家庭 NAS 推荐档；sidecar 向量可选 pgvector 作为高级档。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 存储 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 存储 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 存储 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 存储 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 存储 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 存储 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 存储 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### 插件

签名；沙箱；UI renderer 安全。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 插件 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 插件 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 插件 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 插件 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 插件 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 插件 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 插件 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### 更新

灰度与回滚；迁移前自动备份强制。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 更新 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 更新 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 更新 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 更新 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 更新 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 更新 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 更新 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

### 文档

把本四件套纳入版本发布检查项。 详细步骤：先写 ADR 说明为何现在做；再列 ErrorCode 是否新增；再列迁移是否需要；再列 API summary 是否要进 Scalar；再列 dashboard 路由；再列 CLI 是否暴露；再列 i18n 键；再列单测文件路径；再列集成测试；再列 2HELP 的操作节。拒绝「先做个实验分支直接改 router」的方式。每一个子系统改造都应能单独合并，避免大爆炸 PR。与 2026 趋势的映射在对应趋势章已论述，这里只给工程切口。人员上，领域改动由 infra 所有者审查，UI 由 dashboard 审查，安全由单独 checklist 审查。完成后用可验证命令：相关 pytest 节点 + 手工一条对话。若涉及 schema，更新 test_db_pool 版本断言（仅当已发布才加新 NNN）。

实施子步骤 1：针对 文档 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 2：针对 文档 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 3：针对 文档 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 4：针对 文档 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 5：针对 文档 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 6：针对 文档 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

实施子步骤 7：针对 文档 进行现状调研，打开对应源码目录，列出公共符号与测试缺口，写出最小可行改动（MVP），明确非目标，避免顺手重构。把风险（数据丢失、IM 停摆、密钥泄漏）写在 PR 正文。合并后观察 usage 与 audit 是否出现异常峰值。用户沟通用中文完整句子，不使用内部代号除非用户先用了。

## 分阶段路线（按依赖而非日历）

### 阶段 A：夯实安全与可运维（优先）

安全档向导、HITL 默认策略、备份强制、失败 Top N、文档与 CLI 对齐。

本阶段第 1 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 A：夯实安全与可运维（优先）」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 2 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 A：夯实安全与可运维（优先）」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 3 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 A：夯实安全与可运维（优先）」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 4 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 A：夯实安全与可运维（优先）」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 5 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 A：夯实安全与可运维（优先）」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 6 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 A：夯实安全与可运维（优先）」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 7 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 A：夯实安全与可运维（优先）」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 8 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 A：夯实安全与可运维（优先）」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 9 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 A：夯实安全与可运维（优先）」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 10 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 A：夯实安全与可运维（优先）」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 11 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 A：夯实安全与可运维（优先）」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 12 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 A：夯实安全与可运维（优先）」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

### 阶段 B：任务与评估

Goal/Task 领域、trajectory 升级、失败注入测试、成本分解。

本阶段第 1 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 B：任务与评估」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 2 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 B：任务与评估」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 3 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 B：任务与评估」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 4 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 B：任务与评估」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 5 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 B：任务与评估」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 6 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 B：任务与评估」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 7 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 B：任务与评估」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 8 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 B：任务与评估」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 9 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 B：任务与评估」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 10 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 B：任务与评估」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 11 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 B：任务与评估」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 12 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 B：任务与评估」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

### 阶段 C：团队编排

TeamRun、预算、循环检测、子 Agent 权限收缩、协调者时间线。

本阶段第 1 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 C：团队编排」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 2 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 C：团队编排」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 3 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 C：团队编排」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 4 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 C：团队编排」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 5 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 C：团队编排」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 6 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 C：团队编排」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 7 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 C：团队编排」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 8 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 C：团队编排」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 9 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 C：团队编排」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 10 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 C：团队编排」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 11 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 C：团队编排」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 12 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 C：团队编排」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

### 阶段 D：Computer Use 产品化

GUI 动作纳入策略、流程录制为技能、跨平台桌面测试。

本阶段第 1 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 D：Computer Use 产品化」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 2 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 D：Computer Use 产品化」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 3 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 D：Computer Use 产品化」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 4 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 D：Computer Use 产品化」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 5 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 D：Computer Use 产品化」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 6 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 D：Computer Use 产品化」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 7 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 D：Computer Use 产品化」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 8 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 D：Computer Use 产品化」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 9 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 D：Computer Use 产品化」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 10 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 D：Computer Use 产品化」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 11 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 D：Computer Use 产品化」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 12 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 D：Computer Use 产品化」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

### 阶段 E：记忆与知识治理

ACL/TTL、引用体验、本地模式向导。

本阶段第 1 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 E：记忆与知识治理」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 2 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 E：记忆与知识治理」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 3 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 E：记忆与知识治理」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 4 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 E：记忆与知识治理」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 5 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 E：记忆与知识治理」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 6 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 E：记忆与知识治理」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 7 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 E：记忆与知识治理」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 8 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 E：记忆与知识治理」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 9 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 E：记忆与知识治理」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 10 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 E：记忆与知识治理」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 11 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 E：记忆与知识治理」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 12 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 E：记忆与知识治理」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

### 阶段 F：协议整合

凭证统一、MCP 危险等级、A2A 实验开关。

本阶段第 1 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 F：协议整合」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 2 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 F：协议整合」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 3 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 F：协议整合」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 4 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 F：协议整合」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 5 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 F：协议整合」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 6 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 F：协议整合」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 7 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 F：协议整合」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 8 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 F：协议整合」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 9 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 F：协议整合」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 10 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 F：协议整合」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 11 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 F：协议整合」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 12 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 F：协议整合」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

### 阶段 G：端云路由与语音

本地/云路由、多模态 turn、语音协议。

本阶段第 1 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 G：端云路由与语音」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 2 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 G：端云路由与语音」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 3 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 G：端云路由与语音」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 4 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 G：端云路由与语音」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 5 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 G：端云路由与语音」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 6 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 G：端云路由与语音」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 7 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 G：端云路由与语音」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 8 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 G：端云路由与语音」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 9 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 G：端云路由与语音」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 10 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 G：端云路由与语音」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 11 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 G：端云路由与语音」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

本阶段第 12 个可验证里程碑：交付物必须包含测试与 i18n，演示脚本可在无外网（除 LLM）环境下复现。验收不看 PPT，看：一条命令、一个错误码、一页控制台、一份 2HELP 更新。若里程碑依赖未发布 schema，折叠进当前迁移文件。若里程碑引入新路由，更新 openapi 标签说明。若里程碑改变默认安全行为，CHANGELOG 必须醒目警告。回滚方案：功能开关放 settings，而不是只能靠 git revert 已经迁移的数据。与「阶段 G：端云路由与语音」相关的风险主要是兼容性，需在黄金路径集成测试中加断言。

## 风险、度量与治理

### 风险项 1

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 2

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 3

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 4

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 5

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 6

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 7

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 8

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 9

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 10

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 11

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 12

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 13

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 14

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 15

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 16

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 17

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 18

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 19

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 20

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 21

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 22

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 23

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。

### 风险项 24

描述：演进过程中可能出现范围膨胀、分层被打破、SQLite 与 PG 行为分叉、默认安全变严导致「不好用」、Computer Use 造成真实世界损害、多 Agent 成本爆炸、记忆泄漏家庭隐私、插件供应链。缓解：PR 模板强制填写「分层影响」「安全默认」「测试命令」；公网档与家庭档配置分离；预算在协调者强制；记忆 ACL；插件哈希。度量：工具失败率、平均任务步数、HITL 通过率、每任务 token、备份成功率、平均恢复时长、知识库引用点击率、本地路由占比、P0 安全事件数（应为 0）。治理：继续 feature → develop → release → main；不把实验协议默认打开。沟通：对用户用中文说明「什么变严了、如何一键放宽到内网档」。


## 结语：2026 年的 Octop 应该给人什么感觉

用户打开控制台，不应觉得这是「套了壳的聊天 API」，而应觉得：**家里有一个可问责的数字同事团队**。他们知道自己在做什么任务、花了多少钱、调用了哪些危险工具、记住了什么、忘掉了什么。管理员知道备份能恢复、公网档默认安全、插件不是黑洞。开发者知道分层仍在、测试仍绿、迁移仍成对。

去年的 Octop 已经把自托管助手的「能用」做出来了。今年要把「能托付」做出来。托付需要长任务、评估、治理与默认安全，而不是更多的模型供应商 Logo。本方案所有建议都可以在不放弃单进程模型的前提下进行；若未来真的需要水平扩展，也应是多个完整 Octop 实例加明确的数据所有权，而不是把事件循环偷偷换成外部队列却不改用户故事。

配套阅读：`1DESIGN.md` 约束了什么能改什么不能破；`2HELP.md` 是给用户的操作合同；`SOURCE.md` 是改代码时的地图。
